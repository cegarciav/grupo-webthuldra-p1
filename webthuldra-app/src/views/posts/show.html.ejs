<% if (notice) { %>
    <p><%= notice %></p>
<% } else { %>
    <main>
        <section>
            <article class="post">
                <header>
                    <figure class="foto-usuario-post">
                        <a href="<%= userPath(post.user.id) %>">
                            <img src="<%= post.user.picture %>">
                        </a>
                        <figcaption class="info-usuario">
                            <a href="<%= userPath(post.user.id) %>">
                                <h1><%= post.user.firstName + " " + post.user.lastName %> </h1>
                            </a>
                            <h2>#<%= post.user.tag %></h2>
                        </figcaption>
                    </figure>
                </header>
                <main>
                    <section class="publicacion">
                        <%= post.caption %>
                    </section>
                    <% if (post.media) { %>
                        <section class="foto-post">
                            <img src="<%= post.media%>">
                        </section>
                    <% } %>
                </main>

                <section class="botones">
                    <i class="material-icons">chat_bubble_outline</i>
                    <i class="material-icons">favorite_border</i>
                    <a href="<%= updatePostPath(post.id) %>" title="Actualizar datos de usuario">
                        <i class="material-icons">edit</i>
                    </a>
                    <a href="<%= deletePostPath(post.id) %>" title="Eliminar perfil de usuario">
                        <i class="material-icons">delete_forever</i>
                    </a>
                </section>
            </article>
        </section>
    </main>
<% } %>