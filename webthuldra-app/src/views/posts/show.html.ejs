<% if (notice) { %>
    <p><%= notice %></p>
<% } else { %>
    <h2><%= post.caption %></h2>
    
    <p> </p>
    <p> </p>
    <p> </p>
    <p> </p> 
    <p>Publicado por usuario de id <%= post.postId %></p>
    <img src="<%= post.media %>" alt="Imagen de publicación">

    <section class="post-update-delete">
        <a href="<%= updatePostPath(post.id) %>" title="Eliminar perfil de usuario">
          <i class="material-icons">edit</i>
        </a>
        <a href="<%= deletePostPath(post.id) %>" title="Actualizar datos de usuario">
          <i class="material-icons">delete_forever</i>
        </a>
      </section>
<% } %>